apiVersion: v1
kind: Service
metadata:
  name: {{ include "octo-mesh-demo-app.fullname" . }}
  labels:
    {{- include "octo-mesh-demo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "octo-mesh-demo-app.selectorLabels" . | nindent 4 }}
